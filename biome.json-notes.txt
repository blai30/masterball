// I set this to off for pokeapi stuff because I know the data.
"noNonNullAssertion": "off",

// This is supposed to sort Tailwind classes but it is incomplete so I continue to use Prettier.
"useSortedClasses"

// I turn this off because it's just annoying, I sort the imports myself my way.
"assist": { "actions": { "source": { "organizeImports": "off" } } },

// SVG is used all the time and having to include a title every single time is annoying, who cares.
"noSvgWithoutTitle": "off"
